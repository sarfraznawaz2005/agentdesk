{
  "name": "agentdesk-relay",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "AgentDesk relay — blind WebSocket forwarder (Cloudflare Worker + SQLite Durable Object) for remote web/mobile access.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "dry-run": "wrangler deploy --dry-run",
    "test": "node test/relay-integration.mjs",
    "cf-typegen": "wrangler types"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250601.0",
    "typescript": "^5.6.0",
    "wrangler": "^4.20.0"
  }
}
